services:
  - type: web
    name: gaia-receipt
    env: python
    buildCommand: pip install -r requirements.txt
    startCommand: gunicorn gaia:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.0 